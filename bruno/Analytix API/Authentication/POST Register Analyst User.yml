info:
  name: POST Register Analyst User
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/auth/register"
  body:
    type: json
    data: |-
      {
        "email": "analyst@example.com",
        "password": "Password123!",
        "name": "Analyst User"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
